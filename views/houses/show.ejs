<!doctype html>
<html>

<head>
  <% include ../partials/header %>
</head>
  <h2><%= house.name %></h2>
    <p><img src="<%= house.img_url %>"></p>
     <% students.forEach(student => { %>
    <h2><%= student.fname %> <%= student.lname %></h2>
    <p><img src="<%= student.image %>"></p>
  <% }); %>

</body>
<footer>
  <% include ../partials/footer %>
</footer>

</html>