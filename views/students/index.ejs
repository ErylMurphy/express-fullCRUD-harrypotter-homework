<% include ../partials/header.ejs %>
  <% students.forEach(students => { %>
    <h2><%= students.fname %> <%= students.lname %></h2>
    <a href = "/students/<%=students.student_id %>"><img src="<%= students.image %>"></a>
   <p><%= students.house_id %> </p>
  <% }); %>

<% include ../partials/footer.ejs %>
